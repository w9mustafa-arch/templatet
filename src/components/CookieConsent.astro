---
---

<div
  id="cookieBanner"
  class="fixed bottom-4 left-4 right-4 md:left-auto md:right-6 md:max-w-md z-[99999] hidden slide-up"
>
  <div class="card p-5 backdrop-blur-md bg-white/95 dark:bg-secondary-900/95">

    <!-- Title -->
    <h4 class="mb-2 flex items-center gap-2">
      🍪 We use cookies
    </h4>

    <!-- Text -->
    <p class="text-secondary-600 dark:text-secondary-300 text-sm">
      We use cookies to improve your experience, analyze traffic, and deliver better content.
    </p>

    <!-- Buttons -->
    <div class="flex gap-2 mt-4">
      <button class="btn-primary flex-1" id="acceptCookies">
        Accept
      </button>

      <button class="btn-secondary flex-1" id="rejectCookies">
        Reject
      </button>
    </div>

    <!-- Link -->
    <a
      href="/privacy"
      class="block mt-3 text-xs text-primary-500 hover:underline"
    >
      Learn more
    </a>

  </div>
</div>

<script>
  const banner = document.getElementById("cookieBanner");
  const acceptBtn = document.getElementById("acceptCookies");
  const rejectBtn = document.getElementById("rejectCookies");

  const consent = localStorage.getItem("cookie-consent");

  if (!consent) {
    banner.classList.remove("hidden");
  }

  acceptBtn.addEventListener("click", () => {
    localStorage.setItem("cookie-consent", "accepted");
    banner.classList.add("hidden");

    // هنا تقدر تشغل Google Analytics مثلا
    // loadAnalytics();
  });

  rejectBtn.addEventListener("click", () => {
    localStorage.setItem("cookie-consent", "rejected");
    banner.classList.add("hidden");
  });
</script>
